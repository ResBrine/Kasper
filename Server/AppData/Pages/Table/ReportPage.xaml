<Page x:Class="Pages.Table.ReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pages.Table"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReportPahe">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="CreateBtn" Background="#356adf" Margin="5" Grid.Column="0" Height="30" Padding="10,0" HorizontalAlignment="Center"  Click="CreateByRoomsBtn_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Margin="-2" Background="{TemplateBinding Background}"
              BorderBrush="Transparent"
              BorderThickness="{TemplateBinding BorderThickness}"
              CornerRadius="10"
              Padding="{TemplateBinding Padding}">
                        <TextBlock HorizontalAlignment="Center" Foreground="White"
                VerticalAlignment="Center" FontSize="15"  >Сформировать отчет</TextBlock>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        
    </Grid>
</Page>
